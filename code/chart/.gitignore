# Ignore packaged Helm charts (generated by 'helm package' or 'helmfile repos update')
**/charts/*.tgz

# Ignore temporary directories/files if they are used
temp/
tmp/
*.tmp

# Ignore local configuration/secret files that should not be committed to Git
# This is a common practice when using separate secret management tools like SOPS
# Adjust these patterns based on your project structure and naming conventions
values-secret.yaml
secrets.yaml
# You might also ignore sample environment files if they are just templates
# helmfile/environments/dev/values.yaml
# helmfile/environments/prod/values.yaml

# General OS and Editor files (optional, can be added globally)
.DS_Store
.vscode/
.idea/
*.log

# custom
rendered.yml
rendered.yaml
